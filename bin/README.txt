========================
BUILD OUTPUT DESCRIPTION
========================

To run the project from the command line, paste this folder to the res folder and then
type the following:

java -jar "RoboticonAlchemy.jar" 